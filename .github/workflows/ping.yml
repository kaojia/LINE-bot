name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # 每5分鐘執行一次
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render URL
        run: curl -s https://line-bot-7alv.onrender.com/ > /dev/null
